# WARNING: All content in this file is deleted and replaced when release version is built

# Changing this variable name will break ci/cd pipeline
# since the pipeline injects a variable with same name
# but actual version number for the release
# The pipeline assumes the variable is called SIBEL_VERSION
SIBEL_VERSION = "local"
